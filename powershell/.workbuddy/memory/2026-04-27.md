# 2026-04-27

## powershell_2026 重构完成

- 共生成 26 个 .ps1 文件 + README.md + REFACTORING_CHANGES.md

### 文件清单

```
Lib.ps1               # 核心公共库（Write-Step/Tip/Info/Warn, Invoke-Step/Steps, Assert-Param）
EnvPath.ps1           # PATH 管理（Show-EnvPath, Add-EnvPath）
Profile.ps1           # PowerShell Profile
system/setup.ps1      # 环境初始化（需管理员）
system/check_env.ps1  # 系统信息检查
system/host_info.ps1  # 主机信息
nw/Lib.Route.ps1      # 路由工具库
nw/check.ps1          # 网络接口/WAN IP 检查
nw/dns_check.ps1      # DNS 查询
nw/firewall.ps1       # 防火墙规则
nw/ports.ps1          # TCP 监听端口
nw/reset_net.ps1      # 网络重置（需管理员）
nw/route_switch.ps1   # 路由指标调整
nw/trace_route.ps1    # TraceRoute
hyper-v/check.ps1     # VM 状态检查
hyper-v/enable_hyper-v.ps1
hyper-v/get_cmds.ps1
hyper-v/pnp_dismount.ps1  # DDA 设备卸载
fs/ipad_mtp.ps1       # iPad MTP 文件管理
my/shortcut_desktop.ps1   # 桌面快捷方式批量创建
disk.ps1              # 磁盘信息
top.ps1               # CPU/内存监控
openssh_client.ps1    # OpenSSH 客户端
openssh_server.ps1    # OpenSSH 服务端（需管理员）
eject_removable.ps1   # 弹出可移动磁盘
pnp.ps1               # PnP 设备查询
```

### 规范要点

- Set-StrictMode -Version Latest + $ErrorActionPreference = 'Stop'
- 函数命名 Verb-Noun PascalCase，参数加 [Parameter(Mandatory)] 和类型声明
- #Requires -RunAsAdministrator 替代手动权限检查
- 路径引用使用 $PSScriptRoot，不硬编码
- 别名（?, %, ft, gdr, gwmi）全部替换为完整 cmdlet
- 修复原始代码若干 Bug（ports.ps1 管道断裂、top.ps1 逻辑错误、openssh_server.ps1 typo）
