# 2026-05-15

## env_PATH.ps1 (updated 21:00)
- `/export` — merges System + User PATH, de-dups, sorts, saves to `%USERPROFILE%\bak\PATH.txt`
- `/import` — first backs up current merged PATH to `PATH_bak.txt`, then imports `PATH.txt` into User PATH; skips entries already in PATH_bak or not found on disk
